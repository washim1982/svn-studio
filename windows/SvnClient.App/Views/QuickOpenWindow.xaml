<Window x:Class="SvnClient.App.Views.QuickOpenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Search files, symbols, or commands"
        Height="420" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource ElevatedBrush}"
        BorderBrush="{DynamicResource BorderBrush2}"
        BorderThickness="1">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="0,0,0,1" Padding="14,10">
            <TextBox x:Name="QueryBox"
                     Background="Transparent" BorderThickness="0"
                     Foreground="{DynamicResource TextBrush}"
                     FontSize="14"
                     TextChanged="QueryBox_TextChanged"
                     PreviewKeyDown="QueryBox_PreviewKeyDown"/>
        </Border>

        <ListBox x:Name="ResultsList" Grid.Row="1" Background="Transparent" BorderThickness="0"
                 Margin="6" MouseDoubleClick="ResultsList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="6,4">
                        <TextBlock Text="{Binding DisplayText}" Foreground="{DynamicResource TextBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding SubText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
