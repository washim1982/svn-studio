<Window x:Class="SvnClient.App.Views.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Height="160" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource PanelBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptLabel" Grid.Row="0" Text="Enter a value:" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
        <TextBox x:Name="InputBox" Grid.Row="1" Padding="6" KeyDown="InputBox_KeyDown"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click" Style="{DynamicResource SecondaryButton}"/>
            <Button Content="OK" Width="80" Click="Ok_Click" Style="{DynamicResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
