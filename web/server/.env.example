PORT=4000
# Directory where encrypted settings + upload temp files are stored
DATA_DIR=./data
# Symmetric key used to encrypt stored SVN credentials at rest (32-byte hex recommended)
SETTINGS_SECRET=change-this-to-a-random-32-byte-hex-string
